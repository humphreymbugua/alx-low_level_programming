#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 * @n: The interger that is to be computed.
 *
 * Return: The absolute value of the interger.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
